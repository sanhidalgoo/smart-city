[project]
name = "front"
version = "0.1.0"
description = "Frontend for traffic controller data visualization"
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "flask>=3.0.0",
    "firebase-admin>=6.0.0",
]
